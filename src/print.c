#include "../kernel.h"


void print(const char * str, int color){
    
	j = 0;
	while(vidptr[i] = '\0') {
		i = i + 2;
	}

	i = i + 2;

	while(str[j] != '\0') {
		vidptr[i] = str[j];
		vidptr[i+1] = color;
		++j;
		i = i+2;
	} 
}